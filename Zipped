n,x=map(int,input().split())
m=[]
for _ in range(x):
    a=list(map(float,input().split()))
    m.append(a)
    
for i in range(n):
    total=0
    for j in range (x): 
        total=total+m[j][i]
    
    avg=total/x
    print(avg)

from  itertools import combinations
n=int(input())
letters=input().split()
k=int(input())

all_combinations=list(combinations(letters,k))
count=0
for combo in all_combinations:
    if 'a' in combo:
        count += 1
print("{:.4f}".format(count/len(all_combinations)))

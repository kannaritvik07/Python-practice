import re
n=int(input())
for _ in range(n):
    name,email=input().split()
    email=email.strip("<>")
    pattern=re.match(r'^[A-Za-z][A-Za-z0-9._-]*@[A-Za-z]+\.[A-Za-z]{1,3}$',email)
    if pattern:
        print(name,"<"+email+">")
